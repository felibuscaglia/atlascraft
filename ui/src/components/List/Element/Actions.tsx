import { MoreHorizontal } from "react-feather";

const ListElementActions = () => {
  return (
    <div className="px-2">
      <button>
        <MoreHorizontal size={20} />
      </button>
    </div>
  );
};

export default ListElementActions;
